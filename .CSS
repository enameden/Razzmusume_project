body{
  background:#0b0f1a;
  color:#fff;
  font-family:sans-serif;
  text-align:center;
}

.container{
  max-width:1000px;
  margin:auto;
}

.panel{
  background:#1a2233;
  padding:15px;
  margin:10px 0;
  border-radius:10px;
  box-shadow:0 0 10px #000;
}

h1{
  color:#6cf;
}

button{
  width:120px;
  height:120px;
  border-radius:50%;
  font-size:14px;
  background:radial-gradient(circle,#3f5a88,#1a2233);
  border:2px solid #6cf;
  color:#fff;
  cursor:pointer;
}

button:hover{
  background:#4f6fa8;
}

.race-row{
  margin-bottom:12px;
}

.stat{
  display:grid;
  grid-template-columns:120px 1fr;
  align-items:center;
  margin:6px 0;
}

.stat input{
  justify-self:end;
  text-align:right;
  width:100px;
}

#rouletteBox{
  margin:10px 0;
  padding:15px;
  background:#111;
  border:2px solid #6cf;
  border-radius:10px;
  box-shadow:0 0 10px #6cf;
}

#rouletteLabel{
  font-size:14px;
  color:#aaa;
}

#rouletteDisplay{
  font-size:28px;
  font-weight:bold;
  margin-top:5px;
  color:#fff;
  text-shadow:0 0 10px #6cf;
}

#bottomArea{
  display:flex;
  gap:12px;
  align-items:flex-start;
}

#probBox{
  flex:4;
  min-width:0;
}

#log{
  flex:6;
  min-width:0;
  height:250px;
}

#probBox.panel,
#log.panel{
  box-sizing:border-box;
}

#bottomArea{
  display:flex;
  flex-direction:row;
  gap:12px;
  align-items:flex-start;
}

#probBox{
  flex:4;
}

#log{
  flex:6;
}

.prob-row{
  display:flex;
  align-items:center;
  gap:8px;
  margin:8px 0;
}

.prob-bar{
  width:80px;
  height:10px;
  background:#333;
  border-radius:5px;
  overflow:hidden;
}

.prob-fill{
  height:100%;
  width:25%;
  background:linear-gradient(90deg,#6cf,#3af);
}

.log{
  width:120px;
  overflow:auto;
  text-align:left;
  background:#111;
  padding:10px;
  border-radius:5px;
}

.result{
  font-size:42px;
  font-weight:bold;
  text-align:center;
  transition:0.5s;
}
